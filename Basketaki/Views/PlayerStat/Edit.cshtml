@model Basketaki.ViewModels.PlayerStatFormViewModel

@{
    ViewData["Title"] = "Edit Player Stat";
}

<h1 class="mb-4">Edit Player Stat</h1>

<div class="card shadow-sm">
    <div class="card-body">
        <form asp-action="Edit" method="post">
            <input type="hidden" asp-for="Id" />

            <partial name="_Form" model="Model" />

            <div class="form-text mb-3">
                Shooting made values cannot exceed attempted values.
            </div>

            <button type="submit" class="btn btn-warning">Save Changes</button>
            <a asp-action="Index" asp-route-matchId="@Model.MatchId" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}